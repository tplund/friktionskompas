# Sikkerhedskonfiguration
# Kopier denne fil til .env og generer en sikker secret key

# Flask secret key (generer med: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=din-hemmelige-nøgle-her-brug-token-hex-32

# Development mode
FLASK_DEBUG=True
